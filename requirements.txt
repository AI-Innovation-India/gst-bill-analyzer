# Production-ready requirements - NO compilation needed
# Works on Python 3.11-3.13 with pre-built wheels only

# FastAPI core
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Google Gemini (handles images internally, no PIL needed)
google-generativeai==0.3.0

# PDF text extraction (pure Python)
PyPDF2==3.0.1

# Validation
pydantic==2.5.2

# Environment
python-dotenv==1.0.0

# HTTP
requests==2.31.0
