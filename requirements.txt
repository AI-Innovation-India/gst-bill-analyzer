# Render deployment requirements
# REQUIRES: Python 3.11 (specified in render.yaml as runtime: python3.11)
# Python 3.13 will cause compilation errors with protobuf/pydantic

# Web framework
fastapi==0.104.1
uvicorn==0.24.0

# File uploads
python-multipart==0.0.6

# Google Gemini AI (0.8.0 compatible with Pillow 10.x)
google-generativeai==0.8.0

# PDF extraction (pdfplumber works with Python 3.11)
PyPDF2==3.0.1
pdfplumber==0.10.3

# Image processing (Pillow 10.0.0 has wheels for Python 3.11)
Pillow==10.0.0

# Validation (v2 needs Python 3.11)
pydantic==2.5.2

# Environment
python-dotenv==1.0.0

# HTTP
requests==2.31.0

# Scheduler dependencies (for gst_scheduler.py)
APScheduler==3.10.4
